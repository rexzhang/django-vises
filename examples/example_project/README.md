# Example Project for Django-Vises

## Init Python Venv Environment

- run it at the root of the repository

```shell
pip install -U -e . -r ./requirements.txt
cd examples/example_project
python manage.py runserver
```
